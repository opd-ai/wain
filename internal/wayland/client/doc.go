// Package client manages Wayland compositor connection and protocol synchronization.
// It handles socket connection via $WAYLAND_DISPLAY, global interface binding,
// event synchronization through roundtrip operations, and object lifecycle management.
package client
