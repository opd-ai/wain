// Package dmabuf handles DMA-BUF buffer creation and attachment in Wayland.
// It creates wl_buffer objects from DMA-BUF file descriptors for zero-copy
// GPU rendering with support for DRM fourcc formats.
package dmabuf
