// Package raster provides a software 2D rasterizer for rendering graphics primitives,
// text, and display lists. It includes subpackages for primitives (rectangles, rounded
// rects, lines), curves (Bézier curves, arc fills), compositing (alpha blending, Porter-Duff),
// effects (box shadows, gradients), text (SDF text rendering), display list management,
// and rendering consumers (software and GPU backends).
package raster
